# ------------------------------
# .env.example (copy to .env.local)
# ------------------------------
# Fill these and commit this file (safe placeholders). Do NOT commit .env.local.
# For Vercel Preview builds, set NEXT_PUBLIC_BETTER_AUTH_URL to: https://${VERCEL_URL}
# For Vercel Production, set it to your canonical domain, e.g. https://opencut.yourdomain.com

# --- Database (Supabase Postgres) ---
# Add ?sslmode=require for Vercel/pg to negotiate TLS with Supabase.
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.kilmhwlsqgjxjhvsweqb.supabase.co:5432/postgres?sslmode=require

# --- Better Auth ---
# Public origin for your app (public at runtime).
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000
# Long, random secret (>= 64 chars recommended).
BETTER_AUTH_SECRET=replace-with-a-strong-secret

# --- Runtime ---
# Vercel sets NODE_ENV automatically. Keep locally if you want.
NODE_ENV=development

# --- Rate limiting / caching (choose one) ---
# Local Redis example (for local dev only). Not used on Vercel.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# On Vercel, create a Redis on Upstash and paste REST URL + REST TOKEN there.

# --- Marble CMS ---
# NOTE: Treat MARBLE_WORKSPACE_KEY as server-side (do not expose in NEXT_PUBLIC_*).
MARBLE_WORKSPACE_KEY=cm6ytuq9x0000i803v0isidst
NEXT_PUBLIC_MARBLE_API_URL=https://api.marblecms.com

# --- Freesound (https://freesound.org/apiv2/apply/) ---
FREESOUND_CLIENT_ID=
FREESOUND_API_KEY=

# --- Cloudflare R2 (for auto-captions/transcription) ---
# Cloudflare Dashboard → R2 → Manage R2 API tokens
CLOUDFLARE_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=opencut-transcription

# --- Modal transcription endpoint (after `modal deploy`) ---
# Example format:
#   https://<username>--opencut-transcription.modal.run/transcribe
MODAL_TRANSCRIPTION_URL=https://your-username--opencut-transcription.modal.run/transcribe
